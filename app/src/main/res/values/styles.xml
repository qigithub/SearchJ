<resources>

    <style name="BaseAppStyle" parent="Theme.AppCompat.NoActionBar">

    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="BaseAppStyle">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@null</item>
    </style>


    <style name="LaunchAppTheme" parent="BaseAppStyle">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@mipmap/launch_img</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowContentOverlay">@null</item>

    </style>
    <style name="login_edit">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">@dimen/login_margin</item>
        <item name="android:layout_marginRight">@dimen/login_margin</item>
        <item name="android:maxLines">1</item>
    </style>

    <style name="login_btn">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">@dimen/login_margin</item>
        <item name="android:layout_marginRight">@dimen/login_margin</item>
        <item name="android:maxLines">1</item>
        <item name="android:textSize">18sp</item>
    </style>

</resources>
